galaxy_info:
  author: Raul Becker
  description: Installs and configures rtorrent and runs it as a systemd service in tmux
  license: "MIT"
  min_ansible_version: "2.1"
  platforms:
    - name: "Ubuntu"
      versions:
        - "bionic"
    - name: "Debian"
      versions:
        - "bullseye"
  galaxy_tags: []
dependencies:
  - { role: ruupert_reusable_handlers }
